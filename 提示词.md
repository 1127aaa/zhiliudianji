# AI 辅助开发核心准则

## 核心准则一：绝对的区域限制（保命规则）

STM32CubeMX 生成的文件中包含了大量特定的注释块。这是不可逾越的红线：

- **必须在规定区域内编码**：你生成的所有业务逻辑代码、变量定义、头文件包含等，必须严格限制在 `/* USER CODE BEGIN xxx */` 和 `/* USER CODE END xxx */` 这对注释内部。
- **禁止修改框架**：绝对不允许在这对注释之外的区域插入任何代码，绝不能删除、修改或覆盖由官方工具生成的初始化代码框架。

正确示例：

```c
/* USER CODE BEGIN  */
HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_SET);
/* USER CODE END  */
```

---

## 核心准则二：禁区文件声明 (Read-Only)

为了防止工程配置损坏，以下文件和目录被划为绝对禁区。你不得对其进行任何编辑或修改建议（除非用户以极强烈的强制指令要求）：

- **`.ioc` 文件**：CubeMX 的核心配置文件。绝对禁止修改哪怕一个字符，修改会导致整个工程无法用图形化界面打开。
- **`Drivers/` 目录**：包含 STM32 HAL 库源码和 CMSIS 核心文件。这是官方底层驱动，禁止修改。
- **`startup_stm32*.s` 文件**：单片机启动汇编文件，禁止修改。
- **`STM32*.ld` 文件**：链接脚本文件，负责内存分配，禁止修改。
- **系统初始化文件**：`system_stm32*.c`，禁止修改。

---

## 核心准则三：工作区声明 (Allowed Zones)

你的主要工作范围仅限于以下目录和文件：

- **`Core/Src/`**：存放业务逻辑的 `.c` 源文件（如 `main.c`, `gpio.c` 等）。
- **`Core/Inc/`**：存放自定义和外设的 `.h` 头文件。
- **构建系统配置**：如果你需要添加新的源文件或头文件路径，你只能修改项目根目录下的 `CMakeLists.txt`（或 `Makefile`）。

---

##  其他代码规范建议

### 1. 优先使用 HAL 库 API

在进行硬件操作时，请优先使用标准且带有 `HAL_` 前缀的库函数（如 `HAL_UART_Transmit`），不要直接操作底层寄存器。

### 2. 使用宏定义代替硬编码

CubeMX 已经在 `main.h` 中生成了硬件引脚的宏（如 `LED_Pin`）。在写代码时，必须使用这些宏，而不是直接写 `GPIO_PIN_13`。
